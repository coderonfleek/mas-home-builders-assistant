# US Home Buyer's Research Assistant

### A multi-agent CLI system built with LangChain v1.0 and LangGraph using the Router pattern

---

## 1. Project overview

You're going to build a command-line research assistant that helps a prospective US home buyer evaluate properties and markets. The buyer types a natural-language question — for example, *"Show me 3BR homes under $500k in Austin with good schools"* — and the system produces a **buyer's brief**: a short prose analysis followed by a ranked shortlist of properties.

Under the hood, a **router** classifies the query, decomposes it into targeted sub-questions, and dispatches them **in parallel** to three specialized agents:

- A **Listings agent** that searches a property database
- A **Neighborhood agent** that pulls demographic and housing statistics — from the live US Census API when available, with a bundled JSON snapshot as automatic fallback
- A **Schools agent** that queries a bundled schools dataset for ratings and student-teacher ratios

A final **synthesis node** merges the results into the buyer's brief.

### Why this project?

This is a near-ideal teaching vehicle for the Router pattern because:

- The three verticals are genuinely distinct — different data shapes, different tools, different prompts
- Most realistic buyer queries naturally hit 2–3 agents at once, which is exactly when parallel fan-out with `Send` is worth using
- The synthesis step does real work: it has to join listings to ZIP-level context and reason about trade-offs, not just concatenate
- The domain is tangible — you can evaluate whether the output is actually good by reading it

### What you'll learn

- Building specialized agents with `create_agent` and domain-specific tools
- Defining `TypedDict` state schemas and using **reducers** (`operator.add`) to collect parallel results
- Using **structured output** (Pydantic) to make routing decisions reliable
- Fanning out to multiple agents in parallel with `Send`
- Composing a `StateGraph` with conditional edges
- Designing a **hybrid data path** — live API when available, bundled snapshot as automatic fallback — so tools degrade gracefully
- Consuming `workflow.stream(..., stream_mode="updates")` to render progress live and prove parallel execution
- Shipping a real CLI with nice formatted output

---

## 2. System architecture

### 2.1 High-level flow

```
┌─────────────────┐
│  User query     │  "3BR under $500k in Austin with good schools"
└────────┬────────┘
         │
         ▼
┌─────────────────┐
│   Classifier    │  LLM with structured output → list[Classification]
└────────┬────────┘
         │ Send(...)
         ├──────────────┬──────────────┐
         ▼              ▼              ▼
┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│  Listings    │ │ Neighborhood │ │   Schools    │
│    agent     │ │    agent     │ │    agent     │
│ (SQLite)     │ │ (API+snap)   │ │  (SQLite)    │
└──────┬───────┘ └──────┬───────┘ └──────┬───────┘
       │                │                │
       └────────────────┼────────────────┘
                        │ results (reducer: operator.add)
                        ▼
                ┌───────────────┐
                │  Synthesizer  │  Brief + ranked shortlist
                └───────┬───────┘
                        ▼
                    Final answer
```

### 2.2 Graph nodes

| Node | Role | Input | Output |
|---|---|---|---|
| `classify` | Decide which agents to invoke and with what sub-question | `RouterState` | `{"classifications": [...]}` |
| `listings` | Query the property database | `AgentInput` | `{"results": [{"source": "listings", ...}]}` |
| `neighborhood` | Query Census API, falling back to the bundled snapshot | `AgentInput` | `{"results": [{"source": "neighborhood", ...}]}` |
| `schools` | Query the schools database | `AgentInput` | `{"results": [{"source": "schools", ...}]}` |
| `synthesize` | Build the buyer's brief | `RouterState` | `{"final_answer": "..."}` |

### 2.3 State schema

You'll define four types:

- **`AgentInput`** — Minimal input passed to each specialist (`{query: str}`)
- **`AgentOutput`** — What each specialist returns (`{source: str, result: str}`)
- **`Classification`** — A single routing decision (`{source, query}`)
- **`RouterState`** — The workflow state carried through the graph, with `results` using `Annotated[list, operator.add]` as the reducer

---

## 3. The three verticals

### 3.1 Listings agent

**Purpose:** Search and retrieve property listings.

**Data source:** A realistic synthetic dataset generated by our database builder (Lesson 1.2), loaded into a local SQLite file. Covers five US metros (Austin, Denver, Nashville, Raleigh, Phoenix) with real ZIP codes so the Neighborhood agent (either via the live Census API or the bundled snapshot) returns real demographic data for those ZIPs. Real-world equivalents you can swap in later:

- Kaggle: *"USA Real Estate Dataset"* by Ahmed Shahriar Sakib
- Kaggle: any Zillow/Realtor snapshot with the right column set
- The builder script accepts a `data/raw/listings.csv` with the same schema and skips generation when present

The dataset needs at minimum: `city`, `state`, `zip_code`, `price`, `bedrooms`, `bathrooms`, `sqft`, and ideally `address` and `listing_id`.

**Why static is the right call for teaching:** Real MLS access is gated behind brokerage relationships. A SQLite file students can inspect makes debugging transparent, removes API flakiness from the classroom, and mirrors a legitimate real-world prototyping pattern (many proptech startups work this way).

**Tools:**

- `search_listings(city, state, zip_code, min_bedrooms, max_price, min_sqft, property_type, limit)` — filtered SQL query, returns top N results as a formatted string
- `get_listing_details(listing_id)` — full record for one property

**System prompt emphasis:** The agent should translate loose buyer criteria ("a family home") into concrete filter parameters, and always return the listing IDs so the synthesizer can reference them.

---

### 3.2 Neighborhood agent

**Purpose:** Provide demographic and housing context for a ZIP code or metro.

**Data source:** Hybrid — the **US Census Bureau API** (American Community Survey 5-Year) when available, with a bundled JSON snapshot as fallback.

- API base URL: `https://api.census.gov/data/2022/acs/acs5`
- Snapshot: `data/census_snapshot.json` — ships with the project, covers all 25 ZIPs in our synthetic dataset
- Optional key: https://api.census.gov/data/key_signup.html

**Tools:**

- `get_demographics(zip_code)` — median household income, median age, population
- `get_housing_stats(zip_code)` — median home value, median rent, owner-occupancy rate

**Implementation notes:**

- Census uses ZCTA (ZIP Code Tabulation Area) codes, which align with ZIP codes for our purposes
- The internal `_fetch_acs` helper tries the live API first; on any failure (bad key, timeout, redirect that drops the key, malformed body, or no key configured at all) it falls back to the JSON snapshot with the same return shape
- Cache responses in-memory per session with `functools.lru_cache` — cheap for API calls, still worth doing for snapshot lookups
- Students without a Census key still get real-looking data; students with a working key see live figures. Same tool interface either way

**System prompt emphasis:** The agent should return *interpreted* stats (e.g., "median income in 78704 is $82k, above the Austin metro average") rather than raw numbers, so the synthesizer gets usable context.

---

### 3.3 Schools agent

**Purpose:** Provide school quality signals for a location.

**Data source:** A preloaded schools dataset in the same SQLite file as the listings, generated by our database builder in Lesson 1.2. Real-world equivalents you'd swap in later:

- The **Urban Institute Education Data Portal API** — free, no key, covers the NCES Common Core of Data. Docs at https://educationdata.urban.edu/documentation/
- An NCES Common Core of Data CSV extract loaded the same way we load our synthetic data

**Why SQLite as primary for teaching:** External APIs go down and rate-limit at inconvenient times. Keeping the schools data local means lessons never break mid-recording. The tool interface is designed so swapping in the real API is a one-file change.

**Tools:**

- `search_schools_near(zip_code, level)` — schools in or near a ZIP, filtered by level (`elementary`, `middle`, `high`)
- `get_school_stats(school_id)` — enrollment, student-teacher ratio, and any available performance indicators

**System prompt emphasis:** Schools data is notoriously messy — test scores are missing for many schools, ratings vary by state. The agent must be explicit about confidence and gaps, and prefer structural metrics (student-teacher ratio, enrollment stability) when performance data is unavailable.

---

## 4. The router

### 4.1 Classifier node

A single LLM call using **structured output** (Pydantic) to return a `ClassificationResult` containing a list of `Classification` objects. Each classification has:

- `source` — one of `"listings" | "neighborhood" | "schools"`
- `query` — a sub-question tailored to that source

The classifier prompt explicitly instructs the model to **only include relevant sources** and to rewrite the buyer's query for each source (e.g., the listings agent gets concrete filter criteria, the neighborhood agent gets a ZIP-level question).

### 4.2 Routing function

A `route_to_agents` function that maps each `Classification` to a `Send` object:

```python
return [Send(c["source"], {"query": c["query"]}) for c in classifications]
```

Wired into the graph via `add_conditional_edges`, this gives LangGraph permission to execute the selected agents **in parallel**.

### 4.3 Reducer

`RouterState.results` is `Annotated[list[AgentOutput], operator.add]`, so when the three parallel nodes each return `{"results": [one_output]}`, LangGraph concatenates them into a single list automatically.

---

## 5. The synthesis node — the "buyer's brief"

This is where the project earns its keep. The synthesizer produces a two-part output:

### Part A — Prose brief (2–4 short paragraphs)

- **Market snapshot.** What the dataset and Census show about the area(s) the buyer asked about.
- **Criteria fit.** How the buyer's criteria hold up against the data. Are they realistic? Are there trade-offs?
- **Key trade-offs.** The synthesizer explicitly surfaces tensions — for example, "the sub-$500k listings cluster in ZIPs where the schools have weaker student-teacher ratios than the metro average."

### Part B — Ranked shortlist (top 3–5 properties)

For each property:

- Address, price, beds / baths / sqft
- A one-line **"Why this one"** rationale that references data from at least two of the three agents
- A composite **match score** (0–100) that the synthesizer computes as a weighted blend of:
  - Price fit vs. the buyer's budget
  - School quality signal for the property's ZIP
  - Neighborhood fit (e.g., owner-occupancy rate as a proxy for settled neighborhoods)

The LLM handles the prose. The ranking logic can be either LLM-driven (simpler, less deterministic) or Python-driven (more complex, reproducible). **We'll implement a hybrid:** Python does the scoring math on the structured results, the LLM writes the rationales.

---

## 5.5 The streaming layer

A router that runs three specialists in parallel is only interesting if the user can *see* it happen. Without live progress rendering, the CLI feels like a black box that spins for 10 seconds and dumps a wall of text.

We consume `workflow.stream(..., stream_mode="updates")`, which emits one event per node completion, shaped as `{node_name: state_delta}`. A Rich `Live` panel updates in place through three phases:

- **🧭 Classifying** — flips to "Routed to: X, Y" the moment the classifier lands
- **⚡ Specialists** — a table with per-agent status glyphs (spinner while running, ✓ when done) and elapsed durations
- **📝 Synthesizing** — appears when all picked specialists have finished

When the run completes, the transient panel disappears and a compact summary line stays:

```
🧭 routed → listings, schools   ⚡ agents 2.2s parallel (sum 3.7s)   📝 synth 0.8s
```

The **`parallel` vs `sum`** figures are the critical teaching moment: they're visible proof that `Send` is dispatching concurrently. If a student's parallel time ever equals the sum, something is running sequentially, and the fix is usually a mis-wired conditional edge.

Node-level granularity is deliberately chosen over token streaming — this is a lesson on architecture, not typing effects.

---

## 6. Representative queries and expected routing

| Query | Listings | Neighborhood | Schools |
|---|:-:|:-:|:-:|
| "3BR under $500k in Austin with good schools" | ✅ | ❌ | ✅ |
| "What's the median home price in 78704?" | ❌ | ✅ | ❌ |
| "Best school districts in Nashville under $600k" | ✅ | ❌ | ✅ |
| "Show me 4BR homes in 80210" | ✅ | ❌ | ❌ |
| "Is 37206 a family-friendly ZIP?" | ❌ | ✅ | ✅ |

Note: neighborhood is only invoked when a ZIP is present, so the first query routes to two specialists rather than three. This mix is important — you want students to see the router **omit** irrelevant verticals as well as fan out to multiple.

---

## 7. Tech stack

| Layer | Choice | Reason |
|---|---|---|
| Language | Python 3.11+ | `TypedDict` features, modern type hints |
| Agent framework | `langchain` + `langgraph` (v1.0) | Course focus |
| LLM provider | OpenAI via `init_chat_model` | Default; easy to swap per docs |
| Router model | `gpt-4o-mini` or `gpt-4.1-mini` | Fast, cheap, structured output is reliable |
| Agent model | `gpt-4o` or `gpt-4.1` | Better tool-use reasoning |
| Listings DB | `sqlite3` (stdlib) | Zero setup, inspectable |
| HTTP client | `httpx` | Modern, async-ready if needed later |
| Validation | `pydantic` v2 | For the classifier's structured output |
| CLI rendering | `rich` | Tables, panels, and colored output for the brief |
| Env management | `python-dotenv` | Load API keys |
| Observability (optional) | LangSmith | Inspect traces when routing goes wrong |

---

## 8. Project structure

```
home-buyer-assistant/
├── requirements.txt
├── .env.example
├── .env                       # (gitignored) OpenAI + Census keys
├── data/
│   ├── housing.db             # Built in Lesson 1.2 (listings + schools in one DB)
│   ├── census_snapshot.json   # Bundled Census snapshot for offline fallback
│   └── raw/                   # Optional: drop your own CSVs here
│       └── .gitkeep
├── scripts/
│   └── build_database.py      # Generates synthetic data or loads CSVs into SQLite
└── src/
    ├── __init__.py
    ├── state.py               # TypedDicts + Pydantic schemas
    ├── streaming.py            # Live progress renderer over workflow.stream()
    ├── cli.py                 # Entry point (python -m src.cli)
    ├── tools/
    │   ├── __init__.py
    │   ├── listings.py        # SQLite-backed tools + fetch_listings_raw helper
    │   ├── neighborhood.py    # Hybrid: Census API + snapshot fallback
    │   └── schools.py         # SQLite-backed tools + fetch_schools_summary helper
    ├── agents/
    │   ├── __init__.py
    │   └── specialists.py     # All three create_agent(...) specialists in one file
    ├── router/
    │   ├── __init__.py
    │   ├── classifier.py      # classify_query node
    │   ├── nodes.py           # query_* nodes, route_to_agents, synthesize_results
    │   └── graph.py           # StateGraph assembly
    └── synthesis/
        ├── __init__.py
        ├── scoring.py         # Python-side match scoring
        └── brief.py           # LLM writes the buyer's brief
```

Run with `python -m src.cli`.

---

## 9. Build plan (lesson breakdown)

The project is split into nine lessons. Lesson 1.1 is this plan; lessons 1.2 through 1.9 build the working project; lesson 1.10 is a structured behavior walkthrough with no new code.

Each lesson leaves a runnable checkpoint. Files are written in dependency order — every file's imports resolve at the moment it's written, so there's no going back to fix stubs.

### Lesson 1.2 — Project setup & database

**Goal:** Working Python project with a populated SQLite database.

- Scaffold folders, `.gitignore`, `requirements.txt`, `.env.example`
- Register for an OpenAI API key (required); a Census key is optional — the Neighborhood tool works offline against a bundled snapshot when the key isn't set
- Write `scripts/build_database.py` — generates a realistic synthetic dataset across five US metros with real ZIP codes, or loads CSVs from `data/raw/` if present
- Run the builder and sanity-check the DB

**Deliverable:** `python scripts/build_database.py` produces `data/housing.db` with ~400 listings and ~125 schools.

---

### Lesson 1.3 — State schemas & Listings tools

**Goal:** State types defined, first vertical wired to the DB.

- Define `AgentInput`, `AgentOutput`, `Classification`, `RouterState` in `state.py`
- Define the `ClassificationResult` Pydantic model
- Implement `search_listings` and `get_listing_details` as `@tool` functions
- Implement `fetch_listings_raw` as a plain-Python helper (for the synthesis layer)

**Deliverable:** Both listings tools callable from a REPL.

**Teaching moment:** Why tools return strings and helpers return dicts — and why keeping them separate matters for the scoring code later.

---

### Lesson 1.4 — Neighborhood & Schools tools

**Goal:** All six tools across three verticals working.

- Add the bundled `data/census_snapshot.json` to the project
- Implement the two Census tools (`get_demographics`, `get_housing_stats`) plus the `fetch_neighborhood_raw` helper — all backed by a hybrid fetcher that tries the live API first and falls back to the snapshot
- Implement the two schools tools (`search_schools_near`, `get_school_stats`) plus the `fetch_schools_summary` helper
- Use `functools.lru_cache` on the fetcher and snapshot loader for per-session memoization

**Deliverable:** Every tool verified — works with or without a Census key thanks to the snapshot fallback.

---

### Lesson 1.5 — Specialist agents

**Goal:** Three working agents, each usable in isolation.

- Build `listings_agent`, `neighborhood_agent`, `schools_agent` in a single `agents/specialists.py` module
- Wrap each in `@lru_cache(maxsize=1)` for lazy construction
- Write focused system prompts per vertical

**Deliverable:** Each agent answers a vertical-specific question correctly on its own.

**Teaching moment:** Why specialist system prompts beat one generic prompt — and how narrow prompts + narrow tool sets = predictable behavior.

---

### Lesson 1.6 — Synthesis: scoring & the buyer's brief

**Goal:** Synthesis layer that consumes agent results.

- Implement `synthesis/scoring.py`: deterministic Python match-score math with graceful degradation for missing components
- Implement `synthesis/brief.py`: `build_final_answer` that calls the LLM with agent reports + a pre-scored shortlist, and appends a machine-readable shortlist JSON as an HTML comment
- Verify scoring against real DB data

**Deliverable:** Ranked listings with scores computed deterministically in Python.

**Teaching moment:** When *not* to use the LLM. Scoring is Python (reproducible, cheap, testable); rationales are LLM (natural, contextual).

---

### Lesson 1.7 — The classifier & routing nodes

**Goal:** Router brain and dispatcher.

- Implement `classify_query` with `with_structured_output(ClassificationResult)`
- Include worked examples in the classifier prompt for tricky cases
- Implement `route_to_agents` returning `list[Send]`
- Implement the three specialist-invoking nodes and the `synthesize_results` node

**Deliverable:** Classifier tested standalone on the representative queries from §6.

**Teaching moment:** The contract between the classifier and the downstream routing function — `source` strings must match node names exactly.

---

### Lesson 1.8 — Assembling the StateGraph

**Goal:** Compiled, end-to-end workflow.

- Assemble the `StateGraph` with `add_conditional_edges` pointing from `classify` to the three agent nodes via `route_to_agents`
- Wire all three specialists into `synthesize`
- Compile with `@lru_cache` so CLI startup is fast

**Deliverable:** `workflow.invoke({"query": "..."})` returns a real buyer's brief.

**Teaching moment:** How `Send` + `operator.add` work together, and why `add_conditional_edges` (not `add_edge`) is what enables parallel fan-out.

---

### Lesson 1.9 — Streaming renderer & interactive CLI

**Goal:** A polished interactive experience with visible progress.

- Implement `streaming.py`: consume `workflow.stream(..., stream_mode="updates")` with a Rich `Live` panel showing classification, per-specialist progress, and synthesis
- Print a persistent summary line showing wall-clock parallel time vs. sum of per-agent times (visible proof of parallel execution)
- Implement `cli.py` with a `while True` REPL, preflight checks, `/help`, `/examples`, `/quit`, and `DEBUG=1` env-var support
- Render the buyer's brief with `rich.markdown.Markdown` and the shortlist with `rich.table.Table`

**Deliverable:** `python -m src.cli` opens a prompt where users can ask questions, watch the pipeline run live, and get formatted briefs back.

---

### Lesson 1.10 — Testing the system

**Goal:** Deliberately exercise every routing behavior.

- No new code
- Walk through five queries showcasing three-vertical fan-out, single-vertical routing, two-vertical routing, ZIP-specific queries, and loose-language interpretation
- Use `DEBUG=1` to inspect raw specialist output
- Confirm the `parallel` vs `sum` summary line reflects real concurrency

**Deliverable:** A student who can read the streaming summary and diagnose where a bad brief came from.

---

## 10. Testing strategy

Every lesson has a **verify step** — a short shell one-liner that confirms what was just built actually works before moving on. That's the primary safety net.

Lesson 1.10 is a structured behavior walkthrough: five queries chosen to showcase every important routing property (three-vertical fan-out, single-vertical routing, two-vertical fan-out, ZIP-specific queries, loose language). Running with `DEBUG=1` prints raw specialist output beneath each brief, so students can trace how each piece of the final answer came to be.

The clearest signal that the pipeline is healthy is the streaming summary line's `parallel Xs (sum Ys)` figures — if parallel time ever equals sum, something is running sequentially and the graph is mis-wired.

LangSmith tracing is worth mentioning to students who want to go deeper on debugging, but it's optional for the course.

---

## 11. Known risks and gotchas

| Risk | Mitigation |
|---|---|
| Census API returns no data, is redirected, or the key is expired | The tool falls back to `data/census_snapshot.json` automatically. Same tool interface either way; the agent doesn't know or care which path served the answer |
| Schools API flakiness if you swap to a real one | The current course uses local SQLite, so this isn't an issue during lessons. If you later swap in the Urban Institute API, wrap the call and fall back to SQLite on failure — the tool interface already supports it |
| Classifier routes to the wrong agent | Add explicit examples to the classifier prompt; use structured output (Pydantic), not free-form JSON |
| LLM invents listing IDs in the synthesis step | Always pass the raw listings data to the synthesizer in the prompt, not just the agent's prose response |
| Costs creep up on repeated runs | Router on `gpt-4o-mini`, aggressive result caching at the tool level |
| Student hits OpenAI rate limits during recording | Have a local fallback model path (e.g., Ollama + Llama 3) documented in the README |

---

## 12. Success criteria

By the end of Lesson 1.9, you should be able to:

1. Run `python -m src.cli`
2. Ask *"Show me 3BR homes under $500k in Austin with good schools"*
3. Watch the live panel show the classifier's decision, the specialists ticking through in parallel, and the synthesis step
4. See the summary line report a `parallel` time noticeably lower than the `sum` — visible proof of `Send` dispatching concurrently
5. Get back a two-paragraph market brief and a table of 3–5 ranked listings, each with a rationale that references data from at least two verticals

If you can do that, you've built a real Router-pattern multi-agent system — and you understand why the pattern exists.

---

## 13. Before you start

Make sure you have:

- [ ] Python 3.11+
- [ ] An OpenAI API key (or equivalent — the project is provider-agnostic)
- [ ] A free Census API key *(optional — the Neighborhood tool works offline against a bundled snapshot when no key is present)*
- [ ] Comfort with basic SQL and HTTP requests
- [ ] Read the LangChain Router docs: https://docs.langchain.com/oss/python/langchain/multi-agent/router
- [ ] Skimmed the reference tutorial: https://docs.langchain.com/oss/python/langchain/multi-agent/router-knowledge-base

You do *not* need a Kaggle account — Lesson 1.2 seeds a realistic synthetic dataset so you can start immediately. You can swap in a Kaggle dataset later if you want.

Ready? Move on to Lesson 1.2.